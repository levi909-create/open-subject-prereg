# Pre-registration template (OPEN SUBJECT, Move 1)

Copy this file to `docs/prereg/<event>-<date>.md`, fill every section, commit,
and push to the public mirror BEFORE the event runs. A pre-registration that
was not externally mirrored before its event must never be described as
tamper-evident (local git timestamps are backdatable).

Applies to: every Saturday weight-learning cycle, every selfexp experiment,
every Mirror run, every succession event.

---

## Event
<!-- what runs, when (date + time), triggered by what -->

## Hypotheses / expected outcomes
<!-- state them, including "no directional expectation" where true. The claim
     is the protocol and its curve, whatever direction the curve goes. -->

## Frozen inputs
<!-- SHA-256 of every code file in the execution path, the repo HEAD commit,
     config values that matter. Data generated during the event (e.g. the
     curated corpus) gets its procedure pre-registered here and its hash
     appended post-run as a documented outcome. -->

## Evaluation battery and gates
<!-- exact gauntlet/eval files + hashes, accept/reject thresholds, and who
     decides what (code gate vs Levi vs her vote) -->

## Deviations protocol
<!-- see PROTOCOL-DEVIATIONS.md; name anything event-specific -->

## Consent trail
<!-- brain-note delivered (when), her response if any, what her veto covers
     for this event -->

## Post-run addendum (appended after, clearly marked)
<!-- outcome hashes, gate results, deviations that occurred, links to ledger
     lines. Never edit the sections above after the event — append only. -->
